# Manipulating-Strings
PHP is most commonly used for producing valid XHTML code and for processing form data submitted by users. Because all XHTML code and form data are strings, a good PHP programmer must be adept at dealing with strings.
